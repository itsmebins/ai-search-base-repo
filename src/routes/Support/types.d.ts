export type SupportFormProps = {
  serviceError?: string;
  setService?: (e?: any, op?: any) => void;
  showService?: boolean;
  selectedServices?: any;
};

export type SupportRequestProps = {
  context?: any;
};
